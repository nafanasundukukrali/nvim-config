# vim-config

Для моего комфорта при работе vim-конфиг и быстрый конфиг для установки для c|c++|python|bash.
Конфиг будет изменен для указанного пользователя.
Пример запуска команды:

```
sh install.sh usermem
```

Где `usermem` -- пользователь, для которого нужно перезаписать конфиг .vimrc.
